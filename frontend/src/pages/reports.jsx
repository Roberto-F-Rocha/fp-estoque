import { React, useEffect, useMemo, useState, api, unwrap, fmtMoney, fmtQty, fmtDate, today, getError, Logo, Button, Modal, Toast, Field, EmptyState, Pagination, DataTable, StatusBadge, AlertTriangle, Archive, ArrowDownToLine, ArrowUpFromLine, BarChart3, Bell, Boxes, Check, ChevronDown, CircleDollarSign, ClipboardCheck, Eye, EyeOff, FileDown, FileText, Gauge, History, Layers3, LogOut, Menu, Package, Pencil, Plus, RefreshCw, Search, Settings, ShieldCheck, SlidersHorizontal, Trash2, Truck, UserCog, Users, Warehouse, X, Bar, BarChart, CartesianGrid, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "../shared.jsx";
import { PageHeader } from "../layout.jsx";
export function ReportsPage({ notify }) {
  const [catalog, setCatalog] = useState([]); const [products, setProducts] = useState([]); const [categories, setCategories] = useState([]); const [suppliers, setSuppliers] = useState([]); const [users, setUsers] = useState([]); const [lots, setLots] = useState([]);
  const [filters, setFilters] = useState({ type: "daily_movements", date: today(), start_date: "", end_date: "", product: "", category: "", supplier: "", movement_type: "", user: "", lot: "", stock_status: "", brand: "" }); const [preview, setPreview] = useState(null); const [loading, setLoading] = useState(false);
  useEffect(() => { Promise.all([api.get("reports/"), api.get("products/?page_size=500"), api.get("categories/?page_size=200"), api.get("suppliers/?page_size=500"), api.get("users/?page_size=500").catch(() => ({ data: [] })), api.get("lots/?page_size=500")]).then(([r,p,c,s,u,l]) => { setCatalog(r.data); setProducts(unwrap(p.data)); setCategories(unwrap(c.data)); setSuppliers(unwrap(s.data)); setUsers(unwrap(u.data)); setLots(unwrap(l.data)); }); }, []);
  async function generate() { setLoading(true); try { const r = await api.get("reports/preview/", { params: filters }); setPreview(r.data); } catch (err) { notify(getError(err), "error"); } finally { setLoading(false); } }
  async function download(format) { try { const response = await api.get(`reports/export.${format}`, { params: filters, responseType: "blob" }); const url = URL.createObjectURL(response.data); const a = document.createElement("a"); a.href = url; a.download = `${filters.type}-${today()}.${format}`; a.click(); URL.revokeObjectURL(url); } catch (err) { notify(getError(err), "error"); } }
  return <><PageHeader title="Relatórios" description="Visualize os dados reais do banco antes de exportar em PDF ou CSV." actions={<><Button variant="secondary" icon={FileDown} onClick={() => download("csv")} disabled={!preview}>Baixar CSV</Button><Button icon={FileText} onClick={() => download("pdf")} disabled={!preview}>Baixar PDF</Button></>} />
    <section className="panel report-filters"><div className="form-grid cols-4"><Field label="Relatório"><select value={filters.type} onChange={(e) => setFilters({ ...filters, type: e.target.value })}>{catalog.map((r) => <option key={r.id} value={r.id}>{r.name}</option>)}</select></Field><Field label="Data específica"><input type="date" value={filters.date} onChange={(e) => setFilters({ ...filters, date: e.target.value, start_date: "", end_date: "" })} /></Field><Field label="Data inicial"><input type="date" value={filters.start_date} onChange={(e) => setFilters({ ...filters, start_date: e.target.value, date: "" })} /></Field><Field label="Data final"><input type="date" value={filters.end_date} onChange={(e) => setFilters({ ...filters, end_date: e.target.value, date: "" })} /></Field><Field label="Produto"><select value={filters.product} onChange={(e) => setFilters({ ...filters, product: e.target.value })}><option value="">Todos</option>{products.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select></Field><Field label="Categoria"><select value={filters.category} onChange={(e) => setFilters({ ...filters, category: e.target.value })}><option value="">Todas</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></Field><Field label="Fornecedor"><select value={filters.supplier} onChange={(e) => setFilters({ ...filters, supplier: e.target.value })}><option value="">Todos</option>{suppliers.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select></Field><Field label="Tipo de movimentação"><select value={filters.movement_type} onChange={(e) => setFilters({ ...filters, movement_type: e.target.value })}><option value="">Todos</option><option value="ENTRY">Entrada</option><option value="OUTPUT">Saída</option><option value="ADJ_IN">Ajuste positivo</option><option value="ADJ_OUT">Ajuste negativo</option><option value="REV_IN">Estorno positivo</option><option value="REV_OUT">Estorno negativo</option></select></Field><Field label="Usuário"><select value={filters.user} onChange={(e) => setFilters({ ...filters, user: e.target.value })}><option value="">Todos</option>{users.map((u) => <option key={u.id} value={u.id}>{u.profile?.full_name || u.username}</option>)}</select></Field><Field label="Lote"><select value={filters.lot} onChange={(e) => setFilters({ ...filters, lot: e.target.value })}><option value="">Todos</option>{lots.map((l) => <option key={l.id} value={l.id}>{l.product_name} — {l.number}</option>)}</select></Field><Field label="Marca"><input value={filters.brand} onChange={(e) => setFilters({ ...filters, brand: e.target.value })} placeholder="Todas" /></Field><Field label="Situação do estoque"><select value={filters.stock_status} onChange={(e) => setFilters({ ...filters, stock_status: e.target.value })}><option value="">Todas</option><option value="normal">Normal</option><option value="low">Estoque baixo</option><option value="out">Sem estoque</option></select></Field><div className="form-actions align-end"><Button icon={Eye} onClick={generate} disabled={loading}>{loading ? "Gerando..." : "Visualizar"}</Button></div></div></section>
    {preview && <section className="panel report-preview"><div className="report-preview-head"><div><Logo /><h3>{preview.title}</h3><p>Período: {preview.period} • Gerado em {preview.generated_at} por {preview.generated_by}</p></div></div>{preview.summary && <div className="summary-cards">{Object.entries(preview.summary).map(([key,value]) => <div key={key}><small>{key.replaceAll("_", " ")}</small><strong>{value}</strong></div>)}</div>}{preview.empty_message ? <EmptyState title="Sem dados no período" text={preview.empty_message} /> : <DataTable rows={preview.rows} columns={preview.report_type === "daily_movements" ? [{ key: "time", label: "Data/hora" }, { key: "type", label: "Tipo" }, { key: "product", label: "Produto" }, { key: "code", label: "Código" }, { key: "lot", label: "Lote" }, { key: "previous", label: "Anterior" }, { key: "quantity", label: "Movimentada" }, { key: "final", label: "Final" }, { key: "total", label: "Valor" }, { key: "user", label: "Responsável" }] : preview.columns.map((label, index) => ({ key: String(index), label, render: (row) => row[index] }))} rowKey={preview.report_type === "daily_movements" ? "time" : "_row"} />}</section>}
  </>;
}
